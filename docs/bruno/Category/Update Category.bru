meta {
  name: Update Category
  type: http
  seq: 4
}

put {
  url: {{host}}/categories/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "Coffee Updated",
    "type": "PRODUCT",
    "is_active": true
  }
}
